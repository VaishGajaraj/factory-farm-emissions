# Populate when needed; not required today
# AWS profile/region for future IMI runs
AWS_PROFILE=default
AWS_REGION=us-east-1

# NASA Earthdata credentials are handled via login, not env vars
# GEE authentication is handled via browser OAuth
